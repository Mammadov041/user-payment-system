spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true

app:
  env: test
  logging:
    level: DEBUG
    detailed: true

logging:
  level:
    com.example.userpaymentservice: DEBUG